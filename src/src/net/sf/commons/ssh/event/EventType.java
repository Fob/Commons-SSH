package net.sf.commons.ssh.event;

public enum EventType
{
    SET_PROPERTY,
    PROPERTY_CHANGED,
    UPDATE_CONFIGURABLE,
    INCLUDE_DEFAULT,
    ERROR,
    CLOSING,
    CLOSED,
    CONNECTED,
    AUTHENTICATED,
    OPENNED,
    READ_AVAILABLE;
}
